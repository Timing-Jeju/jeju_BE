# 보안 정책

## 취약점 제보

보안 취약점, 실제 토큰, 개인정보, 재현 가능한 공격 정보는 공개 Issue에 올리지 마세요. 저장소 관리자가 지정한 비공개 연락 채널로 최소한의 재현 정보와 영향 범위를 전달합니다. 비공개 채널이 아직 정해지지 않았다면 저장소 소유자에게 먼저 연락해 안전한 전달 방법을 확인합니다.

## 비밀정보 노출 대응

키, 토큰, 비밀번호, 인증서 또는 Private Key가 노출되면 해당 값을 즉시 폐기하고 재발급합니다. Git 기록에서 파일을 지우는 것만으로는 유출된 자격 증명이 안전해지지 않습니다. 영향 범위를 확인하고 관련 로그와 접근 기록을 검토합니다.

## 저장소 규칙

- 실제 `.env`, `*.pem`, `*.key`, 서비스 계정 JSON을 커밋하지 않습니다.
- 설정과 로그에는 비밀값, Authorization 헤더, 세션 토큰, 개인정보를 출력하지 않습니다.
- `.env.example`에는 `YOUR_API_KEY`, `${API_KEY}` 같은 placeholder만 사용합니다.
- 의심스러운 값은 커밋 전에 Git Hook과 CI secret scan으로 차단합니다.
